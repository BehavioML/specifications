description: |
  Return the successful ICE restart response with updated ICE information and the new ICE session entity-tag identity.

uses:
  - whip/include_ice_session_tag

events:
  - ice_restart_applied
