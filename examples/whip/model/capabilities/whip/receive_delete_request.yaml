description: |
  Receive a DELETE for a known WHIP resource and prepare endpoint-side termination processing.
uses:
  - whip/authorize_request
  - whip/validate_resource_location
  - whip/release_ingest_session
  - whip/delete_whip_resource
events:
  - authorization_succeeded
  - termination_requested
