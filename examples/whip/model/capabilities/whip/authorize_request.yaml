description: |
  Authorize a WHIP HTTP request using bearer-token credentials before applying protocol behavior.
uses:
  - whip/extract_bearer_token
  - whip/validate_bearer_token
requires:
  - auth/bearer_token_validation
events:
  - authorization_succeeded
  - authorization_failed
