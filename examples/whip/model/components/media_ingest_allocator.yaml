description: |
  Implementation boundary for allocating and releasing ingest session capacity without modeling media pipeline internals.

implements:
  capabilities:
    - whip/allocate_ingest_session
    - whip/release_ingest_session
  interfaces:
    - media/ingest_session_allocation

belongs_to: media_ingest
