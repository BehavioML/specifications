# WHIP Example

This WHIP example is being rebuilt from RFC/source behavior using the semantic top-down modeling process.

The previous WHIP BehavioML source model has been intentionally removed so the replacement model can be derived from the RFC/source material rather than refactored from the old model.

Current progress is tracked in [`generated/reports/semantic-top-down-remodel-progress.md`](generated/reports/semantic-top-down-remodel-progress.md).
