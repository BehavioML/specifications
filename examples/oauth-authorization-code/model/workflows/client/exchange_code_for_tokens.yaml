description: |
  The client receives the authorization code through the redirect and exchanges it for tokens at the token endpoint.

roles:
  primary: client
  participants:
    - authorization_server

triggered_by:
  - authorization_code_issued

steps:
  - from: client
    to: authorization_server
    capability: oauth/exchange_code_for_tokens
    label: Exchange code for tokens
