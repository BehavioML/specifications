description: |
  The client uses the issued access token to call the protected resource endpoint.

roles:
  primary: client
  participants:
    - resource_server

triggered_by:
  - tokens_issued

steps:
  - from: client
    to: resource_server
    capability: oauth/call_protected_resource
    label: Call protected resource
