description: |
  Implementation boundary for user-agent redirect mediation in this behavioral model.

implements:
  capabilities:
    - oauth/redirect_to_authorization_server
    - oauth/deliver_authorization_callback

belongs_to: browser_runtime
