description: |
  Coordinate the connection handshake without modeling TLS internals.

uses:
  - connection/validate_peer

requires:
  - crypto/tls_handshake

events:
  - handshake_completed
  - handshake_failed
