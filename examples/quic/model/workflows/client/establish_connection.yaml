description: |
  The client starts a QUIC connection, performs the handshake, and reaches the connected lifecycle state.

roles:
  primary: client
  participants:
    - server

steps:
  - capability: connection/send_initial
    emits:
      - connection_started

  - capability: connection/perform_handshake
    emits:
      - handshake_completed
