description: |
  The client handles a failed QUIC handshake by closing the connection.

roles:
  primary: client
  participants:
    - server

triggered_by:
  - handshake_failed

steps:
  - connection/send_connection_close
  - connection/discard_connection_state
