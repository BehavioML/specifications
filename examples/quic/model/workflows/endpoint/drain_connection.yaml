description: |
  An endpoint observes peer-initiated shutdown and keeps enough state to drain the connection safely.

roles:
  primary: endpoint

steps:
  - from: endpoint
    capability: connection/receive_connection_close
    label: Receive Connection Close
  - from: endpoint
    capability: connection/enter_draining
    label: Enter draining

emits:
  - connection_close_timer_expired
