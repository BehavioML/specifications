description: |
  An endpoint expires a connection after inactivity and discards connection state.

roles:
  primary: endpoint

steps:
  - from: endpoint
    capability: connection/discard_connection_state
    label: Discard connection state

emits:
  - idle_timeout_expired
