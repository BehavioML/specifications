description: |
  An endpoint initiates connection shutdown by sending a close signal and entering the closing lifecycle path.

roles:
  primary: endpoint

steps:
  - capability: connection/send_connection_close
    emits:
      - connection_close_sent

  - capability: connection/enter_closing

  - capability: connection/discard_connection_state
    emits:
      - connection_close_timer_expired
